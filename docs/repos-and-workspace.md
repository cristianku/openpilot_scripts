# Repos and workspace layout

Workspace root: `/Users/cristianku/GitHub/COMMA.AI/CRISTIANKU`

| Repo | Remote | Role |
| --- | --- | --- |
| `opendbc` | `https://github.com/cristianku/opendbc.git` | Car port code: CAN/DBC, `opendbc/car/psa/` Peugeot 3008 interface, carstate, carcontroller, safety. This is where actual porting work happens. |
| `neural-network-data` | `git@github.com:cristianku/neural-network-data.git` | NNLC (neural network lateral control) data for the Sunny variant. Lives under `neural_network_lateral_control/`. |
| `openpilot_scripts` | `https://github.com/cristianku/openpilot_scripts.git` | Setup/merge automation skills + this knowledge hub (`AGENTS.md` and `docs/`). |
| `openpilot` (fork) | `https://github.com/cristianku/openpilot.git` | Generated by the setup scripts. The local checkout is deleted and recreated on every setup run — do not treat it as a durable working copy. |

## Ephemeral setup-scratch folders (do NOT edit or treat as durable)

The `new_openpilot_peugeot_3008`, `new_openpilot_peugeot_3008_sunny`, `new_openpilot_peugeot_3008_testing`, and `new_openpilot_peugeot_3008_sunny_testing` folders under the workspace root are **throwaway scratch checkouts**. The setup skills delete and recreate the mapped one on every run, purely to rebuild openpilot from upstream `master` and bump the `opendbc_repo` (and, sunny, `neural_network_data`) submodule pointer. Their branch/HEAD state is not meaningful and never needs restoring.

The **durable** full openpilot checkout is `SUNNYPILOT/openpilot_sunny` (the 4th folder in `opendbc.code-workspace`) — that is the one to read/develop against. See [../wiki/sources/openpilot_sunny.md](../wiki/sources/openpilot_sunny.md).

## Upstream sources

- comma variant tracks `commaai/openpilot:master`.
- sunny variant tracks `sunnypilot/sunnypilot:master`.
- `panda` is never cloned, updated, staged, or committed by any workflow here.

## PSA / Peugeot 3008 port location

The Peugeot 3008 port lives in `opendbc/opendbc/car/psa/`:
`interface.py`, `carstate.py`, `carcontroller.py`, `psacan.py`, `values.py`, `fingerprints.py`.
The platform is `CAR.PSA_PEUGEOT_3008` in `values.py` (sibling platforms: `PSA_PEUGEOT_208`, `PSA_PEUGEOT_508`).
